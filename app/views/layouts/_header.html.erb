  <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "YP", root_path, class: "navbar-brand"%>
        <%= form_tag("/search/index", method: "get", class: "navbar-form navbar-left" ) do %>
           <div class="form-group center">
            <%= text_field_tag :query, params[:query], class: "form-control", require: true,  style: "width:400px;", id: "business_search", placeholder: "Business name", autocomplete: "off" %>
            </div>
           <%= button_tag(type: "submit", class: "btn btn-primary") do %>
                <i class="fa fa-search"></i>
           <% end %>
        <% end %>   

        <button type="button" style="margin-top:8px;" class="btn btn-primary" data-toggle="collapse" data-target="#filter-panel">
            <i class="fa fa-cog"></i> Advanced Search
        </button>

        <div id="filter-panel" class="collapse filter-panel" style="padding-left:8%;;margin-top:1%;">
            <div class="panel panel-default">
                <div class="panel-body">
                  <%= form_tag("/search/index", method: "get", class: "form-inline" ) do %>
                     <div class="form-group">
                           <%= collection_select :category, :category_id, Category.all, :id, :name, {prompt: "Select Category"}, {class: "form-control", required: true} %>
                        </div><!-- form group [search] -->
                        <div class="form-group">
                           <%= text_field_tag :city, params[:city],class: "form-control input-sm", placeholder: "City" %>                                          
                            <%= text_field_tag :address, params[:address],class: "form-control input-sm", placeholder: "Adress" %>                                   
                        </div> <!-- form group [order by] --> 
                        <div class="form-group"> 
                          <%= button_tag(type: "submit", class: "btn btn-default filter-col") do %>
                            <i class="fa fa-search"></i>
                          <% end %>
                            
                        </div>
                   <% end %>
                </div>
            </div>
        </div>

      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <li>
            <%= link_to  new_suggest_business_path, class: "btn"  do %>
             <i class="fa fa-plus"></i> 
               Suggest Business
             <% end %>
          </li>
         <!-- <li><button type="button" class="btn btn-primary navbar-btn" style="margin-right:10px;" data-toggle="modal" data-target="#SugestModal"> -->
              <!-- Sugest business</button></li> -->
      <% if !current_user %>
        <li><button type="button" class="btn btn-default navbar-btn" style="margin-right:10px;" data-toggle="modal" data-target="#SigninModal">Sign in</button></li>
      <% else %>
        <li class="dropdown">
           <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"> <%= current_user.email %></i><b class="caret"></b></a>
           <ul class="dropdown-menu">
              <li>
                <%= link_to profiles_path  do %>
                  <i class="fa fa-male"></i>
                  Profile
                <% end %>
              </li>
              <% if current_user.admin? %>
                <li>
                  <%= link_to rails_admin_path  do %>
                    <i class="fa fa-lock"></i>
                    Administration
                  <% end %>
                </li> 
              <% end %>
              <li>
                <%= link_to destroy_user_session_path, :method => :delete  do %>
                  <i class="fa fa-sign-out"></i>
                  Sign out
                <% end %>
              </li>
              
            </ul>
          </li>
      <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
    </nav>


     <%= render "layouts/sign_in" %>
     <%# render "layouts/suggest_business" %>






<script type="text/javascript">
  $ ->
  $('#business_search').typeahead
    name: "search"
    remote: "/search/autocomplete?query=%QUERY"

</script>









